# 每日科研雷达交接文档

## 目标

把每日科研雷达的单篇深度分析对齐到用户本地“一多科研”流程：每天筛出的 Top3 论文不仅要有原始 PDF，还要生成真正可读、可追溯、可借鉴的 Paper Card，并导出手机端也能正常阅读的 `文档分析.pdf`。

核心要求：

- Card 不是普通摘要，而是论文逻辑复盘：研究问题、背景路径、方法模块、实验到结论的证据链、局限和可迁移研究想法。
- 输出主体必须是中文科研笔记，保留必要英文术语、模型名、数据集名、指标和公式符号。
- 论文事实使用 `[Paper] [Paper: PDF p. N]` 标记；模型判断使用 `[Analysis]`；研究想法使用 `[Hypothesis]`。
- 不编造作者机构、发表期刊、影响因子、代码地址、数据地址或外部联系；没有证据时写未核验或 Not assessable from supplied material。
- `paper-card.md`、`paper-card.html`、`文档分析.pdf` 都要生成，其中 PDF 优先由 HTML/浏览器打印生成，接近用户本地“一多科研”样例的阅读效果。

## 已核对的本地样例

参考样例：

- `D:\生物\scGPT toward building a foundation model for single-cell multi-omics using generative AI\paper-card.md`
- `D:\生物\scGPT toward building a foundation model for single-cell multi-omics using generative AI\文档分析.pdf`
- `D:\生物\stRGAT identifying spatial domains in spatial transcriptomics via a relational graph attention network\paper-card.md`
- `D:\生物\stRGAT identifying spatial domains in spatial transcriptomics via a relational graph attention network\文档分析.pdf`

观察结论：

- 样例 Card 都是 01-16 节结构，证据指针密集，audit 为 pass。
- 成功样例大量使用表格，尤其在基本信息、痛点、模块拆解、实验链、限制、批判分析和研究想法部分。
- PDF 不是乱码文本堆叠，而是 HTML 风格排版：A4、中文字体、清晰标题、灰色引用块、表格边框、代码样式和分页友好布局。
- 当前 pipeline 旧版 Card 虽有 01-16 节和页码，但中文比例、证据标签和“一多科研”复盘语气不足，因此需要修正。

## 实现机制

每日流程仍然保持：

```text
同步 Zotero embedding 研究库
-> arXiv 抓候选
-> embedding rerank 得到 Top20
-> Qwen 评分选 Top3
-> Top3 下载 PDF
-> 抽取全文
-> 生成 Paper Card
-> 导出 HTML/PDF
-> 上传或链接到 Zotero
-> 生成每日速看和微信推送
```

本次交接重点只修改 Top3 深度分析与 Card/PDF 质量层。

## Card 生成标准

LLM 一次性接收单篇论文全文上下文，输出完整 Markdown。必须严格包含：

```text
## 01 基本信息
## 02 一句话总结
## 03 研究问题
## 04 研究背景与发展路径
## 05 论文识别的核心痛点
## 06 核心思想
## 07 方法总览
## 08 核心模块拆解
## 09 关键公式与符号
## 10 实验设计与证据链
## 11 对结论的正确理解
## 12 作者明确承认的限制
## 13 批判性分析
## 14 学到的知识
## 15 与既有知识的连接
## 16 研究想法
```

格式要求：

- 01 使用 `Field | Value | Source` 表格。
- 05 使用 `Pain point | Manifestation | Cause or author explanation | Evidence from the paper` 表格。
- 08 使用 `Module | Function | Why needed | Input and output | Supporting evidence | Known or expected effect of removal` 表格。
- 10 使用 `Experiment | Claim tested | Comparison and conditions | Result | Supported conclusion | Unsupported stronger conclusion | Source` 表格。
- 12 使用 `Limitation | Specific manifestation | Future direction proposed by authors | Source` 表格。
- 13 使用 `[Analysis] Observation | Potential issue or alternative explanation | Why it matters | How to test it | Basis` 表格。
- 16 每个 idea 包含 name、originating limitation/observation、core hypothesis、delta from paper、initial method、validation、failure modes、innovation status。

## PDF 生成标准

`export_markdown_to_pdf()` 的目标：

- 先把 `paper-card.md` 转为 `paper-card.html`。
- 再用本机或 GitHub runner 可用浏览器无头打印成 `文档分析.pdf`。
- 浏览器不可用时才回退到 PyMuPDF 文本绘制。
- 导出后必须用 PyMuPDF 抽取文本校验，确认 PDF 中包含 `01 基本信息` 且文本长度足够。

浏览器候选：

- `DAILY_PIPELINE_BROWSER`
- Windows Edge / Chrome
- Linux `google-chrome` / `chromium`

## 质量审计

`audit_paper_card()` 必须检查：

- 原始 PDF 是否有效。
- `source_bundle.json` 是否存在且可读。
- `paper-card.md` 是否包含 01-16 全部章节。
- `[Paper: PDF p. N]` 页码是否越界。
- Figure/Table/Equation 是否覆盖输入清单。
- 是否包含 `[Paper]`、`[Analysis]`、`[Hypothesis]`。
- 中文字符是否足够，避免生成英文摘要式 Card。
- 是否仍是 scaffold 或 LLM enrichment 失败产物。

原则：

- 结构错误、PDF 无效、页码越界等为 error。
- 风格不足、证据覆盖不足等为 warning。
- warning 不直接中断 4 点自动流程，但会进入 audit，方便次日追查质量。

## 验收标准

本地验收：

- `uv run pytest tests/test_daily_research_pipeline.py -q` 通过。
- `paper-card.html` 能生成，含表格、引用块和中文字体 CSS。
- `文档分析.pdf` 能被 PyMuPDF 验证，且文本不乱码。
- LLM prompt 测试确认包含“一多科研”、中文科研笔记、`[Paper] [Paper: PDF p. 1]` 和 01 表格要求。

云端验收：

- GitHub Actions 北京时间 04:00 能自动运行。
- Top3 每篇都有原始 PDF、Card Markdown、Card HTML、Card PDF。
- Zotero 云端配额不足时，不阻塞整体流程，而是创建 Zotero item 和链接 note。
- 微信推送中的 PDF/Card 链接能打开真实文件，不是假链接。

## 已知边界

- Zotero 云端文件存储已接近或达到配额时，附件上传可能失败；当前策略是保留 Zotero 条目并写入公开报告链接。
- 影响因子不从 Card 中编造；如果未来需要期刊/影响因子，必须增加外部核验步骤。
- 微信推送只应放精选速看和下载链接；完整证据链以 Card 文件为准。
